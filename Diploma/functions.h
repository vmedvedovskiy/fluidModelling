#pragma once

#include "common.h"

using namespace std;

double f0(double r, double th);

// вектор, содержащий указазтели на все координатные функции, кроме первой
void CreateVectorOfFunctions(vector<coordFunction>& functions);